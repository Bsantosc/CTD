<h2>
  <%= @current_trimester.present? ? "#{@current_trimester.term} - #{@current_trimester.year} Courses" : "No current trimester" %>
</h2>
<ul>
 <% @current_trimester.courses.each do |course| %>
  <li><a href=""><%= course.coding_class.title %></a></li>
 <% end %>
</ul>

<h2>
   <%= @upcoming_trimester.present? ? "#{@upcoming_trimester.term} - #{@upcoming_trimester.year} Courses" : "No upcoming trimester" %>
</h2>
<ul>
 <% @upcoming_trimester.courses.each do |course| %>
  <li><a href=""><%= course.coding_class.title %></a></li>
 <% end %>
</ul>